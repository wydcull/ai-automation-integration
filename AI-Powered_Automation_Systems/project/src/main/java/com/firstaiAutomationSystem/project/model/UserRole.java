package com.firstaiAutomationSystem.project.model;

public enum UserRole {
    ADMIN,      // Full system access, user management
    APPROVER,   // Can approve/reject/send replies
    VIEWER      // Read-only access
}